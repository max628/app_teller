import 'package:flutter/material.dart';

class ThemeColors {
  // PRIMARY
  static const primaryColor = Color(0xFF27292d);
  // SECONDARY COLOR
  static const accentColor = primaryColor;

  //APPBAR
  static const appBarColor = primaryColor;

  //SCAFFOLD
  static const scaffoldBackgroundColor = Colors.white;
  static const backgroundColor = Color(0xFFF5F5F5);
  static const dividerColor = Color(0xFF858585);
  static const cardColor = backgroundColor;
  static const cardTextColor = Color(0xFF989898);

  //ICONS
  static const appbarIconsColor = Colors.white;
  static const iconColor = primaryColor;

  //BUTTON
  static const buttonColor = Color(0xFFff4646);
  static const buttonTextColor = Colors.white;
  static const buttonDisabledColor = Color(0xFFC9C9C9);
  static const buttonDisabledTextColor = Colors.white;

  //TEXT
  static const titleTextColor = Color(0xFF233d63);
  static const appbarTextColor = Colors.white;
  static const bodyTextColor = Colors.black;
  static const Color displayTextColor = bodyTextColor;
  static const Color bodySmallTextColor = bodyTextColor;
  static const hintTextColor = primaryColor;

  //chip
  static const chipBackground = primaryColor;
  static const chipTextColor = Colors.white;

  // list tile
  static const listTileTitleColor = bodyTextColor;
  static const listTileSubtitleColor = bodyTextColor;
  static const listTileBackgroundColor = Colors.white;
  static const listTileIconColor = iconColor;

  // avatar
  static const avatarBgColor = primaryColor;
}
